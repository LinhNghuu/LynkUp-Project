<!DOCTYPE html>
<html lang="en" data-theme="valentine">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <title>Register</title>
  </head>
  <body>
    <header>
      <!-- Include the navbar -->
      <%- include('./partials/navbar', {page: '/'}) %>
    </header>
    <main>
      <!-- Main Content -->
      <div class="md:flex">
        <!-- Left Side (Similar to Login Page) -->
        <div
        class="relative overflow-hidden md:flex w-1/2 bg-gradient-to-r from-red-500 via-pink-500 to-purple-600 justify-around items-center hidden animate__animated animate__fadeInLeft"
      >
        <div class="p-10">
          <h1
            class="text-white font-extrabold text-5xl md:text-6xl font-mono animate__animated animate__fadeInDown"
          >
            Start Your Love Story ✨
          </h1>
          <p
            class="text-white mt-4 text-lg md:text-xl font-serif animate__animated animate__fadeInUp"
          >
            Create your profile and meet soulmates in a community of love, hope, and romance.
          </p>
          <button
            type="submit"
            class="mt-6 bg-white text-red-500 py-2 px-6 rounded-full font-bold mb-2 hover:bg-red-500 hover:text-white transition duration-300 animate__animated animate__bounce"
          >
            <a href="/register">Create Profile</a>
          </button>
        </div>
    
      </div>
      

        <!-- Right Side -->
        <div class="flex md:w-1/2 justify-center py-10 items-center bg-gradient-to-tr from-yellow-400 via-red-400 to-pink-500 animate__animated animate__fadeInRight">
            <div class="bg-white rounded-xl p-5 shadow-lg">
              <% if (typeof successMessage !== 'undefined') { %>
                <h1 class="text-5xl font-bold text-pink-500">Account Created!</h1>
                <p class="py-6">Your account has been successfully created.</p>
                <a href="/login" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded transition duration-300">Proceed to Log in</a>
              <% } else { %>
                <h1 class="text-5xl font-bold text-pink-500">Register</h1>
                <p class="py-6">Please provide your credentials below</p>
  
                <% if (errorMessage) { %>
                  <div class="text-red-600 mb-3"><%= errorMessage %></div>
                <% } %>
  
                <form method="POST" action="/register" class="space-y-6">
                    <div class="flex flex-col">
                        <label for="userName" class="text-sm font-semibold text-gray-600">User Name</label>
                        <input id="userName" name="userName" type="text" placeholder="User Name" required
                            class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition duration-200" 
                            value="<%= typeof userName !== 'undefined' ? userName : '' %>">
                    </div>

                    <div class="flex flex-col">
                        <label for="password" class="text-sm font-semibold text-gray-600">Password</label>
                        <input id="password" name="password" type="password" placeholder="Password" required
                            class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition duration-200">
                    </div>

                    <div class="flex flex-col">
                        <label for="password2" class="text-sm font-semibold text-gray-600">Confirm Password</label>
                        <input id="password2" name="password2" type="password" placeholder="Confirm Password" required
                            class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition duration-200">
                    </div>

                    <div class="flex flex-col">
                        <label for="email" class="text-sm font-semibold text-gray-600">Email Address</label>
                        <input id="email" name="email" type="email" placeholder="Email Address" required
                            class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition duration-200">
                    </div>

                    <input type="submit"
                        class="w-full bg-green-500 text-white p-3 rounded-md cursor-pointer hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-green-500 transition duration-200"
                        value="Register">
                </form>
                <p class="mt-6">Already have an account? <a href="/login" class="text-blue-500 hover:text-blue-600">Login here</a></p>
              <% } %>
            </div>
          </div>
      </div>
    </main>

    <footer>
      <!-- Include the footer -->
      <%- include('./partials/footer') %>
    </footer>
  </body>
</html>
